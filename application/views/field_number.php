
              <form id="<?php echo $parent . "-" . $handle; ?>_form" class="field_form" action="#"><p><label for="<?php echo $parent . "-" . $handle; ?>"><?php echo $title; ?></label></p><div class="input_wrapper"><div class="minus button minus_button"></div><input id="<?php echo $parent . "-" . $handle; ?>" class="num_val default_<?php foreach( $columns as $key => $val ) { if ( $val == 'raw' ){ echo $default_vals[ $key ]; } } ?> input_field number_input" type="number" min=0 value=<?php foreach( $columns as $key => $val ) { if ( $val == 'raw' ){ echo $default_vals[ $key ]; } } ?> data-value=<?php foreach( $columns as $key => $val ) { if ( $val == 'raw' ){ echo $default_vals[ $key ]; } } ?> ><div class="plus button plus_button"></div></div></form>
